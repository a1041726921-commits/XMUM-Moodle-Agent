# 2026-05-01

- Updated the XMUM Moodle Agent desktop GUI so it now opens on a startup page with a Moodle login button.
- Login is now a modal popup; course selection, downloads, and knowledge-note settings remain locked until login succeeds.
- Replaced the old tabbed UI with a left sidebar for Courses, Downloads, and Knowledge Notes.
- Course selection now derives semester choices like `2026/04` and `2025/09` from Moodle course titles and defaults to the latest term.
- Rebuilt `dist/XMUM-Moodle-Agent-Standalone-Fixed.exe` after the UI changes.
- Changed Moodle course discovery to prioritize `https://l.xmu.edu.my/my/courses.php`, request the "All courses" view, expand hidden course cards, and default CLI filtering to all terms instead of `2026/04`.
- Refined course discovery to prefer Moodle's `All (including removed from view)` filter. Removed the GUI download sidebar page, added checkbox course selection with current-term auto-select, added an open-download-folder button, and grouped downloaded files by semester under `data/courses/<term>/`.
